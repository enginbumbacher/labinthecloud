files:
  "/etc/cron.d/nightly_cleanup":
    mode: "000644"
    owner: root
    group: root
    content: |
      0 2 * * * /usr/bin/wget -O /dev/null http://localhost/api/v1/cron

commands:
  remove_old_cron:
    command: "rm -f /etc/cron.d/*.bak"